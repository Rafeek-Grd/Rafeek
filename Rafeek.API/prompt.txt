You are working on the Rafeek project — .NET 8 Clean Architecture with CQRS/MediatR.

══════════════════════════════════════════════════════
CONTEXT: HOW TOKEN GENERATION WORKS IN THIS PROJECT
══════════════════════════════════════════════════════

The chain is:
  SignInCommandHandler
    → _unitOfWork.RefreshTokenRepository.GenerateTokens(user)
      → _jwtTokenManager.GenerateClaimsTokenAsync(user.Email)  ← token expires here

JwtTokenManager is registered as Transient.
JwtSettings is registered as Singleton (loaded from appsettings.json at startup).
JwtSettings has an AccessTokenExpiration property (TimeSpan) used in GenerateClaimsTokenAsync.

The goal: when admin changes SessionTimeoutMinutes in the security dashboard,
ALL NEW tokens issued after that point must respect the new duration —
WITHOUT restarting the server.

══════════════════════════════════════════════════════
WHAT CHANGED FROM THE PREVIOUS DESIGN
══════════════════════════════════════════════════════

  ❌ REMOVED:  IsTwoFactorEnabled  (dropped entirely — not in entity, not in command)
  ✅ KEEP:     SessionTimeoutMinutes — must now SYNC with JWT bearer token lifetime
  ✅ KEEP:     IsForcePasswordChangeEnabled — must now SYNC with ApplicationUser.MustChangePassword
  ✅ KEEP:     Permissions matrix (unchanged)

══════════════════════════════════════════════════════
STEP 1 — DOMAIN CHANGES
══════════════════════════════════════════════════════

A) Rafeek.Domain/Entities/SecuritySetting.cs
   Create this entity (remove IsTwoFactorEnabled vs previous design):

```csharp
using Rafeek.Domain.Common;

namespace Rafeek.Domain.Entities
{
    public class SecuritySetting : BaseEntity
    {
        /// <summary>مهلة الجلسة بالدقائق — تؤثر مباشرة على عمر JWT</summary>
        public int SessionTimeoutMinutes { get; set; } = 15;

        /// <summary>
        /// عند التفعيل: يُجبر جميع المستخدمين النشطين على تغيير كلمة المرور
        /// عند تسجيل الدخول التالي
        /// </summary>
        public bool IsForcePasswordChangeEnabled { get; set; } = false;
    }
}
```

B) Rafeek.Domain/Entities/RolePermission.cs  (unchanged from previous prompt)

C) Rafeek.Domain/Entities/ApplicationUser.cs
   Add ONE new field to the existing class:

```csharp
/// <summary>
/// إذا كانت true، يُجبر المستخدم على تغيير كلمة المرور عند الدخول التالي.
/// يُضبط تلقائياً عند تفعيل IsForcePasswordChangeEnabled من لوحة الأدمن.
/// </summary>
public bool MustChangePassword { get; set; } = false;
```

D) Add to IRafeekDbContext interface:
```csharp
DbSet<SecuritySetting> SecuritySettings { get; }
DbSet<RolePermission> RolePermissions { get; }
```

E) Add implementations to RafeekDbContext + RafeekIdentityDbContext (whichever stores users).

F) Create migration:
   dotnet ef migrations add AddSecuritySettingRolePermissionAndMustChangePassword \
     --project Rafeek.Infrastructure

══════════════════════════════════════════════════════
STEP 2 — SECURITY SETTING CACHE (New Singleton Service)
══════════════════════════════════════════════════════

This is the synchronization bridge between DB and JwtTokenManager.
Because JwtSettings is a singleton and JwtTokenManager is Transient,
we need a shared in-memory store that JwtTokenManager reads at runtime.

A) Create the interface:

FILE: Rafeek.Application/Common/Interfaces/ISecuritySettingCache.cs

```csharp
namespace Rafeek.Application.Common.Interfaces
{
    /// <summary>
    /// Cache in-memory singleton للإعدادات الأمنية.
    /// يُحدَّث فور تغييره من لوحة الأدمن — بدون إعادة تشغيل السيرفر.
    /// </summary>
    public interface ISecuritySettingCache
    {
        /// <summary>مدة عيش الـ JWT (مشتقة من SessionTimeoutMinutes)</summary>
        TimeSpan TokenLifetime { get; }

        /// <summary>هل يجب إجبار المستخدمين على تغيير كلمة المرور؟</summary>
        bool IsForcePasswordChangeEnabled { get; }

        /// <summary>يُستدعى من CommandHandler بعد حفظ الإعدادات في DB</summary>
        void Update(int sessionTimeoutMinutes, bool isForcePasswordChangeEnabled);
    }
}
```

B) Create the implementation:

FILE: Rafeek.Infrastructure/Services/SecuritySettingCache.cs

```csharp
using Rafeek.Application.Common.Interfaces;

namespace Rafeek.Infrastructure.Services
{
    public class SecuritySettingCache : ISecuritySettingCache
    {
        // Defaults — will be overwritten on startup seed or first admin update
        private TimeSpan _tokenLifetime = TimeSpan.FromMinutes(15);
        private bool _isForcePasswordChangeEnabled = false;

        public TimeSpan TokenLifetime => _tokenLifetime;
        public bool IsForcePasswordChangeEnabled => _isForcePasswordChangeEnabled;

        public void Update(int sessionTimeoutMinutes, bool isForcePasswordChangeEnabled)
        {
            _tokenLifetime = TimeSpan.FromMinutes(sessionTimeoutMinutes);
            _isForcePasswordChangeEnabled = isForcePasswordChangeEnabled;
        }
    }
}
```

C) Register as Singleton in DependencyInjection.cs (Infrastructure layer):
```csharp
services.AddSingleton<ISecuritySettingCache, SecuritySettingCache>();
```

D) Seed the cache on startup from DB (in Program.cs, after app.Build() and before app.Run()):

```csharp
// Seed SecuritySettingCache from DB on startup
using (var scope = app.Services.CreateScope())
{
    var dbContext = scope.ServiceProvider.GetRequiredService<IRafeekDbContext>();
    var cache = scope.ServiceProvider.GetRequiredService<ISecuritySettingCache>();

    var setting = await dbContext.SecuritySettings.FirstOrDefaultAsync();
    if (setting != null)
    {
        cache.Update(setting.SessionTimeoutMinutes, setting.IsForcePasswordChangeEnabled);
    }
}
```

══════════════════════════════════════════════════════
STEP 3 — INJECT CACHE INTO JwtTokenManager
══════════════════════════════════════════════════════

FILE: Rafeek.Infrastructure/Identity/JwtTokenManager.cs  (MODIFY existing file)

The current implementation reads token expiry from JwtSettings (singleton from config).
Change it to read from ISecuritySettingCache instead.

Find the line that sets token expiry inside GenerateClaimsTokenAsync, which looks like:
  expires: DateTime.UtcNow.Add(jwtSettings.AccessTokenExpiration)
  OR
  expires: DateTime.UtcNow.AddMinutes(jwtSettings.AccessTokenExpiration.TotalMinutes)

Replace it with:
  expires: DateTime.UtcNow.Add(_securitySettingCache.TokenLifetime)

Constructor injection to add:
```csharp
private readonly ISecuritySettingCache _securitySettingCache;

public JwtTokenManager(
    JwtSettings jwtSettings,
    TokenValidationParameters tokenValidationParameters,
    ISecuritySettingCache securitySettingCache,      // ← ADD THIS
    /* existing params */ )
{
    // ... existing assignments ...
    _securitySettingCache = securitySettingCache;
}
```

IMPORTANT: Keep JwtSettings injection for everything else (Secret, Issuer, Audience, etc.).
Only replace the token expiry/lifetime part with the cache value.

══════════════════════════════════════════════════════
STEP 4 — SYNC IsForcePasswordChangeEnabled → Users
══════════════════════════════════════════════════════

When admin enables IsForcePasswordChangeEnabled:
  → Set MustChangePassword = true on ALL ApplicationUsers (except the current admin)
When admin disables it:
  → Set MustChangePassword = false on ALL users

Also update SignInCommandHandler to return a flag when MustChangePassword is true.

A) Update SignResponse (the DTO returned from sign-in):

FILE: Rafeek.Application/Handlers/AuthHandlers/Commands/SignResponse.cs  (MODIFY)

Add:
```csharp
/// <summary>
/// إذا كانت true، يجب توجيه المستخدم لصفحة تغيير كلمة المرور فوراً.
/// </summary>
public bool MustChangePassword { get; set; } = false;
```

B) Update SignInCommandHandler.cs to set that flag:

After the line:
  signResponse.Role = await currentUser.GetUserRoleStringAsync(...)

Add:
```csharp
signResponse.MustChangePassword = currentUser.MustChangePassword;
```

The AutoMapper mapping already maps AuthResult → SignResponse and ApplicationUser → SignResponse.
MustChangePassword comes from ApplicationUser, so it will be mapped automatically
IF you add it to the SignResponse DTO — confirm the mapper profile covers it,
or add:
  .ForMember(dest => dest.MustChangePassword, opt => opt.MapFrom(src => src.MustChangePassword))
in the ApplicationUser → SignResponse mapping profile.

══════════════════════════════════════════════════════
STEP 5 — COMMAND (Application Layer)
══════════════════════════════════════════════════════

FILE: Rafeek.Application/Handlers/AdminHandlers/Commands/UpdateSecurityDashboard/UpdateSecurityDashboardCommand.cs

```csharp
using MediatR;

namespace Rafeek.Application.Handlers.AdminHandlers.Commands.UpdateSecurityDashboard
{
    public class UpdateSecurityDashboardCommand : IRequest<bool>
    {
        // ── Security Policies (NO IsTwoFactorEnabled) ─────────
        /// <summary>مهلة الجلسة بالدقائق — تُحدِّث مباشرة عمر JWT للطلبات الجديدة</summary>
        public int? SessionTimeoutMinutes { get; set; }

        /// <summary>
        /// عند true: جميع المستخدمين سيُجبَرون على تغيير كلمة المرور عند الدخول التالي.
        /// عند false: يُلغى الإجبار لجميع المستخدمين.
        /// </summary>
        public bool? IsForcePasswordChangeEnabled { get; set; }

        // ── Permissions Matrix ────────────────────────────────
        public List<RolePermissionUpdateItem>? Permissions { get; set; }
    }

    public class RolePermissionUpdateItem
    {
        /// <summary>
        /// "UserManagement" | "FinanceAndReports" | "ContentManagement" | "GeneralSettings"
        /// </summary>
        public string? ModuleKey { get; set; }

        public bool? CanView   { get; set; }
        public bool? CanAdd    { get; set; }
        public bool? CanEdit   { get; set; }
        public bool? CanDelete { get; set; }
        public bool? CanCreate { get; set; }
    }
}
```

══════════════════════════════════════════════════════
STEP 6 — COMMAND HANDLER
══════════════════════════════════════════════════════

FILE: Rafeek.Application/Handlers/AdminHandlers/Commands/UpdateSecurityDashboard/UpdateSecurityDashboardCommandHandler.cs

```csharp
using MediatR;
using Microsoft.AspNetCore.Identity;
using Microsoft.EntityFrameworkCore;
using Rafeek.Application.Common.Interfaces;
using Rafeek.Domain.Entities;

namespace Rafeek.Application.Handlers.AdminHandlers.Commands.UpdateSecurityDashboard
{
    public class UpdateSecurityDashboardCommandHandler
        : IRequestHandler<UpdateSecurityDashboardCommand, bool>
    {
        private readonly IRafeekDbContext _context;
        private readonly ICurrentUserService _currentUser;
        private readonly ISecuritySettingCache _cache;
        private readonly UserManager<ApplicationUser> _userManager;

        public UpdateSecurityDashboardCommandHandler(
            IRafeekDbContext context,
            ICurrentUserService currentUser,
            ISecuritySettingCache cache,
            UserManager<ApplicationUser> userManager)
        {
            _context = context;
            _currentUser = currentUser;
            _cache = cache;
            _userManager = userManager;
        }

        public async Task<bool> Handle(
            UpdateSecurityDashboardCommand request,
            CancellationToken ct)
        {
            // ── 1. Load or seed SecuritySetting ─────────────────
            var setting = await _context.SecuritySettings.FirstOrDefaultAsync(ct);
            if (setting == null)
            {
                setting = new SecuritySetting
                {
                    SessionTimeoutMinutes      = 15,
                    IsForcePasswordChangeEnabled = false
                };
                await _context.SecuritySettings.AddAsync(setting, ct);
            }

            // ── 2. Apply nullable fields ─────────────────────────
            if (request.SessionTimeoutMinutes.HasValue)
                setting.SessionTimeoutMinutes = request.SessionTimeoutMinutes.Value;

            if (request.IsForcePasswordChangeEnabled.HasValue)
            {
                setting.IsForcePasswordChangeEnabled =
                    request.IsForcePasswordChangeEnabled.Value;

                // Sync MustChangePassword on ALL users (except current admin)
                var allUsers = await _userManager.Users
                    .Where(u => u.Id != _currentUser.UserId)
                    .ToListAsync(ct);

                foreach (var user in allUsers)
                    user.MustChangePassword = request.IsForcePasswordChangeEnabled.Value;

                // Batch update via UserManager
                foreach (var user in allUsers)
                    await _userManager.UpdateAsync(user);
            }

            // ── 3. Permissions Matrix ───────────────────────────
            if (request.Permissions != null && request.Permissions.Any())
            {
                foreach (var item in request.Permissions)
                {
                    if (string.IsNullOrWhiteSpace(item.ModuleKey)) continue;

                    var perm = await _context.RolePermissions
                        .FirstOrDefaultAsync(rp => rp.ModuleKey == item.ModuleKey, ct);

                    if (perm == null)
                    {
                        perm = new RolePermission
                        {
                            ModuleKey    = item.ModuleKey,
                            ModuleNameAr = GetModuleNameAr(item.ModuleKey)
                        };
                        await _context.RolePermissions.AddAsync(perm, ct);
                    }

                    if (item.CanView.HasValue)   perm.CanView   = item.CanView.Value;
                    if (item.CanAdd.HasValue)    perm.CanAdd    = item.CanAdd.Value;
                    if (item.CanEdit.HasValue)   perm.CanEdit   = item.CanEdit.Value;
                    if (item.CanDelete.HasValue) perm.CanDelete = item.CanDelete.Value;
                    if (item.CanCreate.HasValue) perm.CanCreate = item.CanCreate.Value;
                }
            }

            // ── 4. Persist to DB ────────────────────────────────
            await _context.SaveChangesAsync(ct);

            // ── 5. Update in-memory cache AFTER DB success ──────
            // This is what makes new JWT tokens pick up the new lifetime immediately
            _cache.Update(
                setting.SessionTimeoutMinutes,
                setting.IsForcePasswordChangeEnabled);

            return true;
        }

        private static string GetModuleNameAr(string key) => key switch
        {
            "UserManagement"    => "إدارة المستخدمين",
            "FinanceAndReports" => "المالية والتقارير",
            "ContentManagement" => "إدارة المحتوى",
            "GeneralSettings"   => "الإعدادات العامة",
            _                   => key
        };
    }
}
```

══════════════════════════════════════════════════════
STEP 7 — ROUTE + CONTROLLER (unchanged structure)
══════════════════════════════════════════════════════

In ApiRoutes.cs — Admin static class, add:
```csharp
public const string UpdateSecurityDashboard = Base + "/admin/security/dashboard";
```

In AdminController.cs:
```csharp
[HttpPut(ApiRoutes.Admin.UpdateSecurityDashboard)]
[Authorize(Roles = "Admin")]
public async Task<IActionResult> UpdateSecurityDashboard(
    [FromBody] UpdateSecurityDashboardCommand command,
    CancellationToken ct)
{
    var result = await _mediator.Send(command, ct);
    return result ? Ok(new { message = "تم تحديث إعدادات الأمان بنجاح" }) : BadRequest();
}
```

══════════════════════════════════════════════════════
SAMPLE REQUESTS
══════════════════════════════════════════════════════

① Change session timeout to 30 minutes — new JWT tokens issued after this
  will expire in 30 minutes:
PUT /api/v1/admin/security/dashboard
{ "sessionTimeoutMinutes": 30 }

② Force all users to change password on next login:
PUT /api/v1/admin/security/dashboard
{ "isForcePasswordChangeEnabled": true }

③ Disable force-change (users can login normally again):
PUT /api/v1/admin/security/dashboard
{ "isForcePasswordChangeEnabled": false }

④ Full save (as user clicks "حفظ التغييرات"):
PUT /api/v1/admin/security/dashboard
{
  "sessionTimeoutMinutes": 15,
  "isForcePasswordChangeEnabled": false,
  "permissions": [
    { "moduleKey": "UserManagement",    "canView": true,  "canAdd": true,  "canEdit": true,  "canDelete": false, "canCreate": true  },
    { "moduleKey": "FinanceAndReports", "canView": true,  "canAdd": false, "canEdit": false, "canDelete": false, "canCreate": false },
    { "moduleKey": "ContentManagement", "canView": true,  "canAdd": true,  "canEdit": true,  "canDelete": false, "canCreate": true  },
    { "moduleKey": "GeneralSettings",   "canView": false, "canAdd": false, "canEdit": false, "canDelete": false, "canCreate": false }
  ]
}

══════════════════════════════════════════════════════
SUMMARY OF ALL FILES TO CREATE / MODIFY
══════════════════════════════════════════════════════

CREATE:
  ✅ Rafeek.Domain/Entities/SecuritySetting.cs
  ✅ Rafeek.Domain/Entities/RolePermission.cs
  ✅ Rafeek.Application/Common/Interfaces/ISecuritySettingCache.cs
  ✅ Rafeek.Infrastructure/Services/SecuritySettingCache.cs
  ✅ Rafeek.Application/Handlers/AdminHandlers/Commands/UpdateSecurityDashboard/UpdateSecurityDashboardCommand.cs
  ✅ Rafeek.Application/Handlers/AdminHandlers/Commands/UpdateSecurityDashboard/UpdateSecurityDashboardCommandHandler.cs

MODIFY:
  ✅ Rafeek.Domain/Entities/ApplicationUser.cs           — add MustChangePassword bool
  ✅ Rafeek.Application/Common/Interfaces/IRafeekDbContext.cs — add 2 DbSets
  ✅ Rafeek.Persistence/RafeekDbContext.cs               — implement new DbSets
  ✅ Rafeek.Infrastructure/Identity/JwtTokenManager.cs   — inject cache, use TokenLifetime
  ✅ Rafeek.Infrastructure/DependencyInjection.cs        — register ISecuritySettingCache as Singleton
  ✅ Rafeek.Application/Handlers/AuthHandlers/Commands/SignResponse.cs — add MustChangePassword
  ✅ Rafeek.API/Program.cs                               — seed cache from DB on startup
  ✅ Rafeek.API/Routes/ApiRoutes.cs                      — add UpdateSecurityDashboard route
  ✅ AdminController.cs                                  — add PUT endpoint

MIGRATION:
  ✅ dotnet ef migrations add AddSecuritySettingRolePermissionAndMustChangePassword \
       --project Rafeek.Infrastructure
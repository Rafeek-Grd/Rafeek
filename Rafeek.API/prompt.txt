You are working on the Rafeek project — a .NET Clean Architecture university management system using CQRS with MediatR.

══════════════════════════════════════════════════════
ARCHITECTURE RULES (follow exactly, no exceptions)
══════════════════════════════════════════════════════

- Layer order: Domain → Application → Infrastructure → Persistence → API
- CQRS: Every feature is a Query (IRequest<T>) + Handler (IRequestHandler<TQuery,TResult>)
- Pagination model class is named `PagginatedResult<T>` (double 'g' — keep this typo, it matches the codebase)
- DbContext interface is `IRafeekDbContext` (injected via constructor)
- Current logged-in user: `ICurrentUserService` — use `.UserId` (type Guid)
- Routes are static string constants in `Rafeek.API/Routes/ApiRoutes.cs`
- All controllers inherit `BaseApiController` and use `[Authorize]`
- Namespace follows folder path exactly

══════════════════════════════════════════════════════
EXISTING CODE TO REUSE (do NOT duplicate)
══════════════════════════════════════════════════════

The DTO already exists at:
  Rafeek.Application/Handlers/AdminHandlers/Queries/GetStudentAcademicRecords/StudentAcademicRecordDto.cs

```csharp
public class StudentAcademicRecordDto
{
    public Guid StudentId { get; set; }
    public string FullName { get; set; } = null!;
    public string UniversityEmail { get; set; } = null!;
    public string UniversityCode { get; set; } = null!;
    public string? DepartmentName { get; set; }
    public float Cgpa { get; set; }
    public string AcademicStatus { get; set; } = null!;   // "Stable" | "Warning" | "Probation"
    public string AcademicStatusLabel { get; set; } = null!;
    public int Level { get; set; }
    public int Term { get; set; }
}
```

The Admin query already exists at:
  Rafeek.Application/Handlers/AdminHandlers/Queries/GetStudentAcademicRecords/GetStudentAcademicRecordsQuery.cs

```csharp
public class GetStudentAcademicRecordsQuery : IRequest<PagginatedResult<StudentAcademicRecordDto>>
{
    public string? AcademicStatus { get; set; }
    public string? SearchTerm { get; set; }
    public float? Cgpa { get; set; }
    public Guid? DepartmentId { get; set; }
    public int PageNumber { get; set; } = 1;
    public int PageSize { get; set; } = 10;
}
```

The Admin route already exists in ApiRoutes.cs:
  Admin.GetStudentAcademicRecords = Base + "/admin/students/academic-records"

══════════════════════════════════════════════════════
DOMAIN RELATIONSHIPS (use for EF queries)
══════════════════════════════════════════════════════

Student → has UserId (FK to ApplicationUser), DepartmentId, Level, Term, AcademicProfileId
StudentAcademicProfile → has CGPA, GPA, Standing, CompletedCredits, RemainingCredits
ApplicationUser → has FirstName/LastName (or FullName), Email, UniversityCode
Department → has Name
Doctor (Professor entity) → has UserId, DepartmentId, IsAcademicAdvisor
LectureGroup → has DoctorId (FK to Doctor), CourseId
Enrollment → has StudentId, LectureGroupId, CourseId, Status
Staff → exists as DbSet<Staff> Staffs in IRafeekDbContext

Available DbSets on IRafeekDbContext:
  Students, StudentAcademicProfiles, Departments, LectureGroups,
  Enrollments, Doctors, Staffs, Grades

══════════════════════════════════════════════════════
TASK: CREATE THE FOLLOWING FILES
══════════════════════════════════════════════════════

─────────────────────────────────────
1. ADMIN — Complete the missing Handler
─────────────────────────────────────

FILE: Rafeek.Application/Handlers/AdminHandlers/Queries/GetStudentAcademicRecords/GetStudentAcademicRecordsQueryHandler.cs

- Implement IRequestHandler<GetStudentAcademicRecordsQuery, PagginatedResult<StudentAcademicRecordDto>>
- Inject: IRafeekDbContext, ICurrentUserService
- Query scope: ALL students across ALL departments (no restriction)
- Join path:
    _context.Students
      .Include(s => s.User)
      .Include(s => s.Department)
      .Include(s => s.AcademicProfile)
- Apply filters (all optional):
    • SearchTerm → WHERE User.FullName CONTAINS OR UniversityCode CONTAINS OR User.Email CONTAINS
    • AcademicStatus → WHERE AcademicProfile.Standing == AcademicStatus
    • Cgpa → WHERE AcademicProfile.CGPA == Cgpa
    • DepartmentId → WHERE Student.DepartmentId == DepartmentId
- Map to StudentAcademicRecordDto:
    • FullName ← User.FullName (or FirstName + " " + LastName depending on entity)
    • UniversityEmail ← User.Email
    • UniversityCode ← Student.UniversityCode
    • DepartmentName ← Department?.Name
    • Cgpa ← AcademicProfile.CGPA
    • AcademicStatus ← AcademicProfile.Standing  (raw value)
    • AcademicStatusLabel ← map Standing to Arabic label:
        "Stable"    → "منتظم"
        "Warning"   → "تحذير"
        "Probation" → "إنذار أول"
        default     → Standing
    • Level ← Student.Level
    • Term ← Student.Term
- Apply Skip/Take pagination using PageNumber and PageSize
- Return PagginatedResult<StudentAcademicRecordDto>

Also add the endpoint to AdminController:
  Route: ApiRoutes.Admin.GetStudentAcademicRecords  [HttpGet]
  Send GetStudentAcademicRecordsQuery from query parameters ([FromQuery])
  Requires [Authorize(Roles = "Admin")] (or the role string used in this project)

─────────────────────────────────────
2. STAFF — Full new feature
─────────────────────────────────────

FILES TO CREATE:

a) Rafeek.Application/Handlers/StaffHandlers/GetStudentAcademicRecords/GetStaffStudentAcademicRecordsQuery.cs

```csharp
public class GetStaffStudentAcademicRecordsQuery
    : IRequest<PagginatedResult<StudentAcademicRecordDto>>
{
    public string? AcademicStatus { get; set; }
    public string? SearchTerm { get; set; }
    public float? Cgpa { get; set; }
    public Guid? DepartmentId { get; set; }
    public int PageNumber { get; set; } = 1;
    public int PageSize { get; set; } = 10;
}
```

b) Rafeek.Application/Handlers/StaffHandlers/GetStudentAcademicRecords/GetStaffStudentAcademicRecordsQueryHandler.cs

- Same handler logic as Admin (Staff has system-wide read access)
- Query scope: ALL students (no department restriction — Staff oversees the whole institution)
- Identical filter + mapping + pagination logic as the Admin handler above
- Reuse StudentAcademicRecordDto from the AdminHandlers namespace
  (add using Rafeek.Application.Handlers.AdminHandlers.Queries.GetStudentAcademicRecords;)

c) Add route constant in ApiRoutes.cs inside the existing Staff static class:
```csharp
public const string GetStudentAcademicRecords = Base + "/staff/students/academic-records";
```

d) Add endpoint to StaffController:
  Route: ApiRoutes.Staff.GetStudentAcademicRecords  [HttpGet]
  Send GetStaffStudentAcademicRecordsQuery from [FromQuery]
  Requires Staff role authorization

─────────────────────────────────────
3. PROFESSOR — Full new feature (scoped to own sections only)
─────────────────────────────────────

FILES TO CREATE:

a) Rafeek.Application/Handlers/InstructorHandlers/Queries/GetStudentAcademicRecordsPagginated/GetStudentAcademicRecordsForInstructorQuery.cs

```csharp
public class GetStudentAcademicRecordsForInstructorQuery
    : IRequest<PagginatedResult<StudentAcademicRecordDto>>
{
    /// <summary>Optional: filter to a specific LectureGroup the professor teaches</summary>
    public Guid? LectureGroupId { get; set; }
    public string? AcademicStatus { get; set; }
    public string? SearchTerm { get; set; }
    public float? Cgpa { get; set; }
    public int PageNumber { get; set; } = 1;
    public int PageSize { get; set; } = 10;
}
```

b) Rafeek.Application/Handlers/InstructorHandlers/Queries/GetStudentAcademicRecordsPagginated/GetStudentAcademicRecordsForInstructorQueryHandler.cs

- Inject: IRafeekDbContext, ICurrentUserService
- SCOPING LOGIC (critical — professors only see their own students):
  Step 1: Resolve Doctor from current user:
    var doctor = await _context.Doctors
        .FirstOrDefaultAsync(d => d.UserId == _currentUserService.UserId);
    if (doctor == null) throw NotFoundException or return empty result

  Step 2: Get all LectureGroup IDs belonging to this Doctor:
    var lectureGroupIds = await _context.LectureGroups
        .Where(lg => lg.DoctorId == doctor.Id)
        .Select(lg => lg.Id)
        .ToListAsync();

  Step 3: If LectureGroupId filter is provided, validate it belongs to this doctor:
    if (request.LectureGroupId.HasValue)
        lectureGroupIds = lectureGroupIds
            .Where(id => id == request.LectureGroupId.Value).ToList();

  Step 4: Get distinct StudentIds enrolled in those LectureGroups:
    var studentIds = await _context.Enrollments
        .Where(e => lectureGroupIds.Contains(e.LectureGroupId))
        .Select(e => e.StudentId)
        .Distinct()
        .ToListAsync();

  Step 5: Query Students filtered to those IDs:
    var query = _context.Students
        .Where(s => studentIds.Contains(s.Id))
        .Include(s => s.User)
        .Include(s => s.Department)
        .Include(s => s.AcademicProfile)
        .AsQueryable();

- Apply filters (same as Admin/Staff):
    SearchTerm, AcademicStatus, Cgpa
    (NO DepartmentId filter for Professor — they are already scoped to their students)

- Map to StudentAcademicRecordDto (same mapping as Admin handler)
- Apply pagination and return PagginatedResult<StudentAcademicRecordDto>
- Reuse StudentAcademicRecordDto from AdminHandlers namespace

c) Add route constant in ApiRoutes.cs inside the existing Professor static class:
```csharp
public const string GetStudentAcademicRecords = Base + "/professor/students/academic-records";
```

d) Add endpoint to the Professor controller (check if it's named ProfessorController):
  Route: ApiRoutes.Professor.GetStudentAcademicRecords  [HttpGet]
  Send GetStudentAcademicRecordsForInstructorQuery from [FromQuery]
  Requires Professor/Instructor role authorization

══════════════════════════════════════════════════════
SHARED IMPLEMENTATION NOTES
══════════════════════════════════════════════════════

Pagination pattern to follow (match PagginatedResult<T> constructor used elsewhere):
  - Count total with .CountAsync() before Skip/Take
  - Apply .Skip((PageNumber - 1) * PageSize).Take(PageSize)
  - Return new PagginatedResult<StudentAcademicRecordDto>(items, totalCount, PageNumber, PageSize)

AcademicStatusLabel mapping helper (put inline in each handler or extract to a private method):
  "Stable"    → "منتظم"
  "Warning"   → "تحذير"
  "Probation" → "إنذار أول"
  default     → the raw Standing string

Do NOT create a new DTO — always reference:
  Rafeek.Application.Handlers.AdminHandlers.Queries.GetStudentAcademicRecords.StudentAcademicRecordDto

Do NOT register handlers manually — MediatR auto-discovers them via
  DependencyInjection.cs AddMediatR assembly scanning.

Use async/await throughout. Use CancellationToken ct parameter in Handle().

Follow the exact same file/folder naming conventions used in:
  Rafeek.Application/Handlers/InstructorHandlers/Queries/GetStudentsInSectionPagginated/
  (notice "Pagginated" with double 'g')

══════════════════════════════════════════════════════
SUMMARY OF FILES TO CREATE / MODIFY
══════════════════════════════════════════════════════

CREATE:
  ✅ Rafeek.Application/Handlers/AdminHandlers/Queries/GetStudentAcademicRecords/GetStudentAcademicRecordsQueryHandler.cs
  ✅ Rafeek.Application/Handlers/StaffHandlers/GetStudentAcademicRecords/GetStaffStudentAcademicRecordsQuery.cs
  ✅ Rafeek.Application/Handlers/StaffHandlers/GetStudentAcademicRecords/GetStaffStudentAcademicRecordsQueryHandler.cs
  ✅ Rafeek.Application/Handlers/InstructorHandlers/Queries/GetStudentAcademicRecordsPagginated/GetStudentAcademicRecordsForInstructorQuery.cs
  ✅ Rafeek.Application/Handlers/InstructorHandlers/Queries/GetStudentAcademicRecordsPagginated/GetStudentAcademicRecordsForInstructorQueryHandler.cs

MODIFY:
  ✅ Rafeek.API/Routes/ApiRoutes.cs — add Staff.GetStudentAcademicRecords and Professor.GetStudentAcademicRecords constants
  ✅ AdminController.cs — add GET endpoint for existing Admin query
  ✅ StaffController.cs — add GET endpoint for new Staff query
  ✅ ProfessorController.cs — add GET endpoint for new Professor query
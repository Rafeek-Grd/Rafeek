Use this prompt with ChatGPT, Claude, Cursor, Copilot, or any AI code reviewer:

Act as a Senior .NET Backend Architect and Code Reviewer.

Analyze the entire project and find ALL "Get All" handlers/endpoints/services/repositories that return collections of data.

Your tasks:

1. Scan the whole solution and identify:
   - MediatR Query Handlers
   - Service methods
   - Repository methods
   - Controller endpoints
   - Specification queries
   - Any method that returns IEnumerable, List, ICollection, IQueryable, PaginatedResult, PagedResponse, or similar collection types.

2. For every Get All method:
   - Check whether pagination is implemented.
   - Check whether PageNumber and PageSize are used correctly.
   - Check whether large datasets may be loaded into memory.
   - Check whether CountAsync() and Skip/Take are applied correctly.
   - Check whether sorting and filtering are preserved.

3. Apply the following business rule consistently across the entire project:

   Normal behavior:
   - If PageNumber > 0:
     - Return paginated data.
     - Apply Skip and Take.
     - Return pagination metadata.

   Special behavior:
   - If PageNumber == -1:
     - Ignore pagination.
     - Return ALL available records.
     - Do NOT apply Skip or Take.
     - Preserve filtering and sorting.
     - Return the complete dataset.

4. Verify that:
   - Existing filters still work.
   - Existing search functionality still works.
   - Existing sorting still works.
   - Existing includes/navigation properties still work.
   - Existing DTO mapping still works.
   - Existing authorization rules are not affected.

5. Refactor duplicated pagination logic:
   - Extract reusable pagination functionality if possible.
   - Follow Clean Architecture principles.
   - Follow SOLID principles.
   - Avoid code duplication.

6. For each modified file provide:
   - File name
   - Current implementation
   - Problem found
   - Required change
   - Final corrected code

7. Detect potential issues:
   - Performance problems
   - N+1 queries
   - Memory issues when PageNumber = -1
   - Missing AsNoTracking()
   - Inefficient CountAsync() usage
   - Multiple database roundtrips

8. Generate the final implementation so that every Get All endpoint follows this exact logic:

```csharp
if (request.PageNumber == -1)
{
    var allData = await query
        .OrderBy(...)
        .ToListAsync(cancellationToken);

    return Result.Success(allData);
}

var totalCount = await query.CountAsync(cancellationToken);

var data = await query
    .Skip((request.PageNumber - 1) * request.PageSize)
    .Take(request.PageSize)
    .ToListAsync(cancellationToken);

return new PaginatedResult<T>(
    data,
    totalCount,
    request.PageNumber,
    request.PageSize);
Produce a final report containing:
Total Get All methods found
Methods already compliant
Methods requiring modification
Exact code changes for each method
Any architectural improvements recommended

Do not skip any file. Review the entire solution thoroughly and ensure the PageNumber = -1 behavior is implemented consistently everywhere.